Install Node Modules by using Install yarn 
Then Install Material Ui Dependancy by Using npm install @mui/material @emotion/react @emotion/styled
then start the application yarn start/npm start
# DesignFestFrontEnd
